export interface IDevice   {
    id: string,
    name: string,
    last_app: string,
    last_ip_address: string,
    last_heard: string,
    product_id: number,
    connected: false,
    system_firmware_version: string
  }